{
  "permissions": {
    "allow": [],
    "deny": []
  },
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd /tmp/cc-agent/66919772/project && npm run build 2>&1 | tail -5; exit_code=${PIPESTATUS[0]}; if [ $exit_code -ne 0 ]; then echo '{\"systemMessage\": \"Build FAILED — run npm run build to see errors.\"}'; exit 2; fi",
            "timeout": 30,
            "statusMessage": "Verifying build..."
          }
        ]
      }
    ]
  }
}
